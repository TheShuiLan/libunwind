name: 手动编译完整 libunwindstack（Android）

on:
  workflow_dispatch:
    inputs:
      target_arch:
        description: '编译目标架构 (Android ABI)'
        required: true
        default: 'arm64-v8a'
        type: choice
        options:
          - arm64-v8a
          - armeabi-v7a
          - x86_64
          - x86

permissions:
  contents: write
  actions: write
  packages: read

jobs:
  build-android:
    runs-on: ubuntu-24.04
    steps:
      - name: 安装基础依赖
        run: |
          sudo apt update && sudo apt install -y \
            cmake ninja-build git python3

      - name: 下载并配置 Android NDK
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r26c-linux.zip
          unzip -q android-ndk-r26c-linux.zip
          echo "NDK_HOME=$PWD/android-ndk-r26c" >> $GITHUB_ENV
          echo "$PWD/android-ndk-r26c/toolchains/llvm/prebuilt/linux-x86_64/bin" >> $GITHUB_PATH

      # 修复点 1：改回完整克隆，确保包含所有汇编文件
      - name: 克隆 libunwindstack 源码
        run: |
          git clone https://android.googlesource.com/platform/system/unwinding
          cd unwinding/libunwindstack

      - name: 创建 CMakeLists.txt（启用 Dex/JIT）
        working-directory: unwinding/libunwindstack
        run: |
          cat > CMakeLists.txt << 'EOF'
          cmake_minimum_required(VERSION 3.19) # 修复点 2：提高版本号以消除 NDK 弃用警告
          project(libunwindstack VERSION 1.0.0 LANGUAGES C CXX ASM) # 修复点 3：显式启用 ASM 语言支持

          set(CMAKE_CXX_STANDARD 17)
          set(CMAKE_CXX_STANDARD_REQUIRED ON)
          set(CMAKE_POSITION_INDEPENDENT_CODE ON)

          # 头文件路径
          include_directories(
            ${CMAKE_CURRENT_SOURCE_DIR}/include
            ${CMAKE_CURRENT_SOURCE_DIR}
            ${CMAKE_CURRENT_SOURCE_DIR}/utils
          )

          # 所有源文件
          set(SOURCES
            AndroidLogStdout.cpp
            AndroidUnwinder.cpp
            ArmExidx.cpp
            Demangle.cpp
            DexFile.cpp
            DexFiles.cpp
            DwarfCfa.cpp
            DwarfEhFrameWithHdr.cpp
            DwarfMemory.cpp
            DwarfOp.cpp
            DwarfSection.cpp
            Elf.cpp
            ElfInterface.cpp
            ElfInterfaceArm.cpp
            Global.cpp
            JitDebug.cpp
            LogAndroid.cpp
            LogStdout.cpp
            MapInfo.cpp
            Maps.cpp
            Memory.cpp
            MemoryXz.cpp
            Regs.cpp
            RegsArm.cpp
            RegsArm64.cpp
            RegsRiscv64.cpp
            RegsX86.cpp
            RegsX86_64.cpp
            Symbols.cpp
            ThreadEntry.cpp
            ThreadUnwinder.cpp
            Unwinder.cpp
          )

          # 根据架构选择对应的汇编文件
          if(ANDROID_ABI STREQUAL "arm64-v8a")
            list(APPEND SOURCES AsmGetRegsArm64.S)
          elseif(ANDROID_ABI STREQUAL "armeabi-v7a")
            list(APPEND SOURCES AsmGetRegsArm.S)
          elseif(ANDROID_ABI STREQUAL "x86_64")
            list(APPEND SOURCES AsmGetRegsX86_64.S)
          elseif(ANDROID_ABI STREQUAL "x86")
            list(APPEND SOURCES AsmGetRegsX86.S)
          endif()

          # 禁用 XZ 支持以避免寻找 liblzma（修复点 4）
          option(ENABLE_XZ "Enable Xz support" OFF)
          if(NOT ENABLE_XZ)
            list(REMOVE_ITEM SOURCES MemoryXz.cpp)
          endif()

          # 构建静态库
          add_library(unwindstack STATIC ${SOURCES})

          # 链接库
          target_link_libraries(unwindstack log)

          # 导出头文件
          target_include_directories(unwindstack PUBLIC
            $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
            $<INSTALL_INTERFACE:include>
          )

          install(TARGETS unwindstack ARCHIVE DESTINATION lib)
          install(DIRECTORY include/ DESTINATION include)
          EOF

      - name: 编译 libunwindstack（完整版）
        env:
          ABI: ${{ github.event.inputs.target_arch }}
        working-directory: unwinding/libunwindstack
        run: |
          rm -rf build install
          mkdir build && cd build

          case ${ABI} in
            arm64-v8a)
              TOOLCHAIN_ARGS="-DANDROID_ABI=arm64-v8a -DCMAKE_SYSTEM_NAME=Android -DCMAKE_ANDROID_ARCH_ABI=arm64-v8a"
              ;;
            armeabi-v7a)
              TOOLCHAIN_ARGS="-DANDROID_ABI=armeabi-v7a -DCMAKE_SYSTEM_NAME=Android -DCMAKE_ANDROID_ARCH_ABI=armeabi-v7a"
              ;;
            x86_64)
              TOOLCHAIN_ARGS="-DANDROID_ABI=x86_64 -DCMAKE_SYSTEM_NAME=Android -DCMAKE_ANDROID_ARCH_ABI=x86_64"
              ;;
            x86)
              TOOLCHAIN_ARGS="-DANDROID_ABI=x86 -DCMAKE_SYSTEM_NAME=Android -DCMAKE_ANDROID_ARCH_ABI=x86"
              ;;
          esac

          cmake .. -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE=${NDK_HOME}/build/cmake/android.toolchain.cmake \
            -DANDROID_PLATFORM=android-23 \
            ${TOOLCHAIN_ARGS}

          ninja
          mkdir -p ../install/lib
          cp libunwindstack.a ../install/lib/
          cp -r ../include ../install/

          echo "========== 安装目录内容 =========="
          find ../install -type f

      - name: 上传静态库产物到 Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: libunwindstack-${{ github.event.inputs.target_arch }}-static
          path: unwinding/libunwindstack/install/lib/libunwindstack.a
          if-no-files-found: error
          retention-days: 90
